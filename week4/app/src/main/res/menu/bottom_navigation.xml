<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/navigationPlay"
        android:icon="@drawable/ic_action_play"
        android:title="@string/playing"
        android:enabled="true"
        app:showAsAction="always"/>

    <item
        android:id="@+id/navigationRating"
        android:icon="@drawable/ic_action_rating"
        android:title="@string/rating"
        android:enabled="true"
        app:showAsAction="always"/>

    <item
        android:id="@+id/navigationFavorite"
        android:icon="@drawable/ic_action_favorite"
        android:title="@string/favorites"
        android:enabled="true"
        app:showAsAction="always"/>
</menu>